# プロローグとは
* コンパイラが関数の先頭に出力する定型の命令の事。
```assembly
push rbp
mov rbp,rsp
sub rsp,16 ←この数値は関数毎の変数の個数やサイズに合せた値にする。
```
* ベースポインタの設定。スタックポインタをローカル変数分進める処理等。
* コンパイラが関数の末尾に出力する定型の命令は [エピローグ](./epilogue)と言う。
